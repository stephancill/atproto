[build]
builder = "dockerfile"
dockerfilePath = "services/pds/Dockerfile.railway"

[deploy]
healthcheckPath = "/xrpc/_health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
